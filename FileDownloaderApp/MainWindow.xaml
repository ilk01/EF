<Window x:Class="FileDownloaderApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Downloader" 
        Width="700" Height="500"
        WindowStartupLocation="CenterScreen"
        Background="#2c3e50 ">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите URL-адреса файлов (через запятую):" 
                   FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBox Name="UrlInput" Width="450" Height="30" Margin="0,0,10,0" FontSize="14"/>
            <Button Content="Скачать файлы" Width="150" Height="30" Click="OnDownloadButtonClick" FontSize="14"/>
        </StackPanel>

        <ListBox Name="StatusListBox" Grid.Row="2" Height="250" FontSize="14" Margin="0,0,0,10"/>

        <ProgressBar Name="ProgressBar" Grid.Row="3" Height="25" Width="Auto" Foreground="DarkGreen" Margin="0,0,0,10"/>

        <TextBlock Name="CompletionMessage" Grid.Row="4" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="DarkSlateGray"/>
    </Grid>
</Window>
